# Dependencies
node_modules
.pnp
.pnp.js

# Next.js build output
.next
out
next-env.d.ts
.swc/

# Production build
build
dist

# Environment variables (never commit secrets; keep .env.example)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editors & OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local note data (local backend only; not committed). The app auto-creates
# this directory on first save, so it doesn't need to exist in git.
data/
